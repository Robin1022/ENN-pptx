# 公司视觉规范（从 `templates/company-template.pptx` 实测解析）

来源文件：`templates/company-template.pptx`（新奥新智 ENNEW 品牌）。该文件未随本仓库分发，需按 [`templates/README.md`](templates/README.md) 自行放置。它本身是三页写满内容的真实示例，**没有清空**——它同时承担两个角色：(1) 提供可实例化的空白版式，(2) 作为视觉风格参照（配色/字体/图形语言怎么用）。下面的数值都是从模板 XML 里实测出来的，不是估的。

## 画布

- `12192000 x 6858000` EMU = **13.33in x 7.5in**（16:9 宽屏）
- 用 pptxgenjs 时对应 `LAYOUT_WIDE`，**不是**默认的 `LAYOUT_16x9`（10in x 5.625in）——新建 `pres.layout` 时必须显式设置，否则右侧/下方内容会画到画布外（写入但不显示，见原版 pptx skill 的踩坑清单）

## Logo —— 自动继承，不要手动加

Logo 图片在 `slideMaster1` 里，不在单页 slide 里。凡是新页面用模板自带的 9 个版式（`slideLayout1.xml` ~ `slideLayout9.xml`）中任意一个实例化，logo 会自动出现在右上角，**不需要、也不应该**在新页面里再手动插入一次 logo 图片。

- 位置：`(10.56in, 0.21in)`，尺寸 `2.41in x 0.85in`
- 只有当新页面是用 pptxgenjs **完全从零**画（不经过模板版式）时，才需要手动补 logo；这种情况尽量避免，优先走"从版式实例化"的路径

## 配色 —— 按页面主题换强调色，不是单一主色贯穿全篇

模板的 theme1.xml 是 PowerPoint 默认色板（未被启用），实际配色是每页直接写在 shape 上的硬编码 `srgbClr`。三个示例页各自用了不同的强调色，规律是**强调色跟随页面主题**，不是固定不变的品牌主色：

| 用途 | 取值 | 出现场景 |
|---|---|---|
| 蓝色强调（组织/认知类内容） | `2B5BD7`（主）/ `0070C4` / `2156A2` | slide1：认知拆解类内容 |
| 青绿强调（技术/架构类内容） | `2E9482`（主）/ `48A198` | slide2：技术架构图 |
| 警示/高亮红 | `FF0000` | slide3：能力框架图里的强调点 |
| 中性背景 | `FFFFFF`（主背景）| 全篇 |
| 中性灰（分隔/次要文本） | `CFD8DC` / `ECEFF1` / `607D8B` | 表格线、辅助说明 |
| 浅蓝辅助块 | `B4C7E7` / `9DC3E6` | 卡片底色 |

**新页面选强调色的原则**：先判断这页内容更偏"组织/认知/客户"类还是"技术/架构/能力"类，分别用蓝色系或青绿色系做强调色；不要把两种强调色混在同一页里，也不要为了"好看"引入模板里没出现过的颜色。如果内容类型都不沾边（比如财务、时间线），默认用蓝色系（`2B5BD7`），因为它在 3 页里出现频率最高、最接近这份模板的默认基调。

## 字体

- 中文正文/标题主字体：**微软雅黑**（`微软雅黑`，slide3 用得最多）
- 中文次选：**等线**（`等线`，slide1/2 用得更多，效果和微软雅黑接近，两者混用无妨，不要引入第三种中文字体）
- 英文/数字：`Arial`（少量出现，用于纯数字/英文标签）
- 图标符号：`Wingdings`（用于项目符号图形，不要手打这个字体名去写正文）

这几款都是 Office/WPS 中文环境的标配字体，LibreOffice 视觉 QA 渲染时不会有宽度替换问题，可以放心信任 QA 阶段的文字溢出检查结果。**不要**套用原版 pptx skill 文档里那份英文 safe-font 列表（Arial/Calibri/Cambria 等）作为中文正文选择——那份列表是给英文场景的，中文场景一律微软雅黑/等线。

## 可用版式一览（全部属于 `slideMaster1`，都会自动带 logo）

| 文件 | 名称 | 占位符 | 适用场景 |
|---|---|---|---|
| `slideLayout1.xml` | 封面页 | `ctrTitle` + `subTitle` | 首页 |
| `slideLayout2.xml` | 目录页 | 半宽正文区 (`idx=1`) | 目录/议程 |
| `slideLayout3.xml` | 正文页 | `title` + 页码 | **最常用**——标题已定位好，正文区空白，需要自己画内容（模板里 3 个示例页用的都是这个） |
| `slideLayout4.xml` | 1_封面页 | `ctrTitle` | 封面备选（无副标题） |
| `slideLayout5.xml` | 1_正文页 | `title` + 页码 | 正文页备选 |
| `slideLayout6.xml` | 1_仅标题 | `title` + 页码 | 只需要一个大标题、正文完全自由发挥时用 |
| `slideLayout7.xml` | 标题和内容 | `title` + 通用内容区 (`idx=1`) + 页码 | **唯一带"通用内容占位符"的版式**——纯文字要点类页面（不需要复杂图形）优先用这个，而不是在 `正文页` 上从零画文本框 |
| `slideLayout8.xml` | 正文-蓝色 | 无占位符 | 蓝底通体背景，适合整页大图/纯视觉页 |
| `slideLayout9.xml` | 1_标题幻灯片 | 两个 `body` 占位符，无 `title` | 双栏正文对比，标题另想办法（或干脆不要标题） |

## 内容页的可选原型：三种结构模块

`slide1.xml`/`slide2.xml`/`slide3.xml` 三页的正文内容都是**深度嵌套的自由图形组**（`<p:grpSp>` 套多层 `<p:sp>`/`<p:pic>`），不是版式占位符驱动的内容——但它们各自对应这家公司 PPT 的一种标准结构模块（反思模块 / 技术图谱模块 / 技术方案详情模块），槽位定义、复用边界和自主选择方法写在 [`MODULES.md`](MODULES.md) 里。

新建正文内容页时，先在 `PLANNING.md` 规定的整份 PPT 规划中确定该页的信息任务，再由模型自主选择：
- 内容语义匹配三种模块之一 → 复制对应源页改造
- 不匹配任何模块 → 从合适的公司版式实例化页面，再按本文档的配色、字体和图形语言搭建内容

三个模块不要全部强制使用，也不要让用户逐页选模块。选择的标准是页面要表达的逻辑是否与原型匹配。

模板中的 `slide1.xml`/`slide2.xml`/`slide3.xml` 包含真实示例文案，只能作为工作阶段的复制源和风格参照。复制完所有目标页后，必须把未被用户明确要求保留的原始示例页从最终 `<p:sldIdLst>` 移除，并运行 `clean.py`。不得将三页示例内容带入交付文件。

## 常见图形语言(从三页示例里归纳,新画内容页时可以借用)

- 编号圆角徽标(① ② ③ 这种带序号的小圆点/圆角矩形),标记流程步骤或对应关系
- 分层框架图:用不同深浅的色块堆叠表示"数据层/语义层/应用层"这类分层架构
- 图标 + 短句并排的信息行(不是大段项目符号文字墙)
- 卡片式布局:浅色块承载一组信息,卡片之间留白分隔,不用色条分隔
