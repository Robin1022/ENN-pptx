# 内容模块定义 —— 由模型按内容自主选用

模板 `templates/company-template.pptx` 里的 3 个示例正文页(`slide1.xml`/`slide2.xml`/`slide3.xml`)代表这家公司 PPT 常用的**三种可选内容原型**——反思模块 / 技术图谱模块 / 技术方案详情模块。它们是页面规划时可用的表达方式,不是每页必须套用的模具,也不是要求用户做的三选一。

先按 `PLANNING.md` 完成整份 PPT 的叙事和逐页规划,然后由模型根据每页的内容逻辑自主判断是否引用这三种原型。缺少受众、目标、业务逻辑或交付约束时,先做出最合理的推理或假设并明确标记,再继续规划;只有无法合理推断且不补充就无法继续时才追问。不要把版式或模块选择推回给用户。

## 核心原则:内容决定结构,模板决定风格

这三个模块**不是三个只能填文字的固定模具**。栏数、每栏条目数、架构图分几层、对比图怎么组织——这些结构性的东西应该服务于用户实际要表达的内容,内容需要几栏就画几栏,需要几条就放几条,不能为了省事把内容硬塞进跟原图一模一样的形状数量里。如果这个 skill 只会在数量凑巧对得上时替换文字、对不上就将就,那跟用户自己手动在 PowerPoint 里改文字没有本质区别——这不是这个 skill 存在的意义。

**语义匹配 ≠ 几何复制。** 选中某个模块只表示该页的论证逻辑与原型匹配,不代表可以沿用源页的栏宽、容器高度和形状数量。复用前必须按 `LAYOUT.md` 比较各区域的条目数、文字量和主次;密度明显不平衡时,改栏宽、改上下结构或重画整个正文区。

反过来,**风格必须和模板保持一致**:配色(见 `COMPANY_STYLE.md` 配色表)、字体(微软雅黑/等线)、Logo、卡片/图标/编号徽标这套图形语言、留白和间距习惯,新画的任何结构都要沿用,不能自由发挥出一套跟模板对不上的视觉风格。三个示例页存在的意义,与其说是"可复制的容器",不如说是"这套图形语言长什么样"的参照样板——圆角卡片+虚线边框+编号圆点/圆角矩形徽标+蓝色或青绿色的标题条,这些视觉习惯要延续,但装多少内容、分几份,看用户给了什么。

**判断走哪条路径**:
- 用户内容的条目数/结构跟源页面(slide1/2/3)基本一致 → 复制源页,做"安全文字替换"(见 `SKILL.md`"填充模板时"一节),这是最快、最不容易出错的路径,结果也最贴近模板原有的精细排版
- 用户内容的条目数、分栏方式、层级跟源页面对不上,或用户明确要求不同的结构 → 在保留视觉语言(配色/字体/图标风格/卡片样式)的前提下,增删对应的形状子组,或者用 pptxgenjs 按 `COMPANY_STYLE.md`"常见图形语言"一节的范式重新搭建这部分——不要因为"默认复用"就把内容削足适履

## 模块一:反思模块(对应 `slide1.xml`)

- **主标题**:一句话点出本页核心议题(问题陈述式,例如"客户认知——AI Agent 落地企业,倒逼数据技术架构的重新定义")
- **副标题**:一句话概括"从什么到什么"的转变或矛盾
- **正文分栏**(源页面是左中右三栏——左「认知」/中「痛点总结」/右「技术方案」,这是*典型*结构,不是唯一答案):
  - 用户内容天然对应"认知 / 痛点 / 方案"三类信息 → 沿用三栏
  - 用户只有两类信息(比如没有单独的"认知"部分,直接是痛点+方案)→ 改两栏,不要硬凑出第三栏内容
  - 每栏的条目数按用户实际给的条数来,不用凑成原图的 3-4 条——原图的条数只是参考,不是上限或下限
- 强调色:蓝色系 `2B5BD7`(见 `COMPANY_STYLE.md` 配色表)
- **结构调整方式**:条目数、分栏数跟源页面接近时,复制 `slide1.xml` 做文字替换最快;需要增减条目或改分栏数时,在同一栏内复制/删除对应的卡片形状子组(保持同样的圆角矩形标题条+虚线框样式),栏数变化则需要重新分配三栏/两栏的宽度和间距,必要时用 pptxgenjs 按 `COMPANY_STYLE.md` 的图形语言重画整个正文区

## 模块二:技术图谱模块(对应 `slide2.xml`)

- **主标题**:技术升级/跃迁的主张(例如"基于需求技术再出发——重构平台,跃迁本体")
- **副标题**:一句话概括"从底层到上层"的技术路径
- **左右结构**:
  - 左:技术架构图,体现分层,每层标注提供的核心价值/能力
  - 右:技术目标和手段,按层对应列出关键技术名称 + 一句话说明
- 强调色:青绿色系 `2E9482`
- **结构调整方式**:左侧这张分层架构图**不是锁死不能动的**。用户的技术方案在分层数量、组件构成上跟原图(数据层/本体层,Hive/Spark/Flink 等具体技术栈)一致或接近时,复用原有分层框架改文字/数字最省事;只要分层数、组件类型对不上,或用户明确要求体现自己的架构,就应该重新设计——按 `COMPANY_STYLE.md`"常见图形语言"一节的分层框架图范式(色块分层堆叠、编号徽标标注价值点)重新画,层数、每层的组件数量都按用户实际的技术方案来,不要因为"默认复用"就把不匹配的架构硬塞进原有图形组。右侧技术面板同理:面板数、每个面板的条目数按用户给的技术点数量来

## 模块三:技术方案详情模块(对应 `slide3.xml`)

- **主标题**:能力/框架主张
- **副标题**:一句话概括这页要证明的核心结论
- **左右结构**:
  - 左:新旧方案对比图,**必须基于具体场景**(不能是抽象的"方案A vs 方案B"),清晰画出"传统方式 vs 新方式"在这个场景下的具体差异点
  - 右:方案亮点和先进性总结,突出相比旧方案的优势,可用编号徽标强调(见 `COMPANY_STYLE.md` 常见图形语言)
- 强调色:默认延续蓝色系,除非左侧对比图内容明显偏技术架构类(此时可用青绿色系)
- **结构调整方式**:对比图里的场景元素(旧方案/新方案的具体画面)必须按新场景重新设计,**不要复用原图的图标语义**——原图是"数据治理/AI用数"场景专属的隐喻,套到别的场景上会文不对题。这不只是换个标签文字:如果新场景的对比逻辑跟原图的层级/元素数量对不上(比如原图 8 个场景元素,新场景只需要 5 个,或者对比维度跟"数据源→本体层→用数方案"这条链路不一样),要相应增减形状、调整对比图的层级结构,保持"新旧对比+具体场景"这个表达逻辑和整体图形语言(色块、编号徽标、连线风格)即可,不必强求元素数量或层级跟原图一致

## 模块选择流程（整体规划阶段由模型执行）

1. 先判断页面的信息任务:提出问题与解法、展示技术分层,还是证明新旧方案差异。
2. 与三种原型的表达逻辑匹配时,在逐页规划中直接标明拟使用的模块;不匹配时选择其他公司版式,不硬套。
3. 内容结构与源页接近时,用 `add_slide.py` 复制后做安全文字替换;结构不同时,按实际内容增删形状子组或重搭对应区域。
4. 所有目标页复制完成后,将原始示例页从 `<p:sldIdLst>` 移除,再运行 `clean.py`;最终 deck 不得残留模板示例文案。
5. 生成后按 `SKILL.md` 完成内容、文件和视觉 QA。

## 三个模块之外的内容

如果某页内容明显不属于这三种原型(比如封面、目录、纯数据图表页),不要往这三个模块里硬套——在整体规划中直接选用 `COMPANY_STYLE.md` 版式一览表里的其他版式(`slideLayout1` 封面 / `slideLayout2` 目录 / `slideLayout7` 标题和内容 等)。
